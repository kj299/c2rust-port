[package]
name = "cjson_ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
# libcjson_rs.so — the drop-in C-ABI surface over the safe core.
name = "cjson_rs"
crate-type = ["cdylib"]

[dependencies]
cjson_core = { path = "../core" }

[lints]
workspace = true
