# Build + capture artifacts (regenerable by oracle/run.sh); sources are committed.
oracle/cjson_oracle
# Fuzz-only corrected oracle: cJSON_Utils.c with the one-line decode fix, built by
# build_fixed.sh from make_fixed_utils.py. Both the generated .c and its binary are
# regenerable and never committed (the pristine vendored source is the only truth).
oracle/cjson_oracle_fixed
oracle/cJSON_Utils_fixed.c
oracle/corpus/
oracle/reports/
rust/target/
reports/
libcjson_c.so
libcjson_rs.so
reports_ffi.json
